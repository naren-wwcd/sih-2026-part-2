# Leave unset to use the dev proxy (/api -> http://localhost:8000, see vite.config.ts).
# Set this for production builds or if your backend isn't on localhost:8000.
# VITE_API_BASE_URL=http://localhost:8000
